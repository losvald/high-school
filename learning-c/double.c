#include <stdio.h>

int main(void) {
  double a = 2.0;
  double b = 3.0;
  double d;
  char c;
  int *p;
  d = a + b;

  printf ("2 + 3 = %g", (a + b));
  scanf ("%c", &c);
  while ((c= getchar()) != 'a')
  return 0;
  }
