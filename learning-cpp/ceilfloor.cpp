#include <math.h>
#include <iostream.h>
#include <conio.h>
int main() {
    float a = 2.54, b = 5.03, c = 1.98;
    cout << " ceil " << ceil(a) << " floor " << floor(a) << endl;
    cout << " ceil " << ceil(b) << " floor " << floor(b) << endl;
    cout << " ceil " << ceil(c) << " floor " << floor(c) << endl; 
    getch();
    return 0;
}     
